version: "3.9"

services:
  devopsdb:
    image: nagaraju444/dbimage:v1
    ports:
      - "3306:3306"
    deploy:
      replicas: 2
    networks:
      - mynetwork

  application:
    image: nagaraju444/appimage:v1
    ports:
      - "1111:8080"
    deploy:
      replicas: 3
    depends_on:
      - devopsdb
    networks:
      - mynetwork

networks:
  mynetwork:
    driver: overlay
